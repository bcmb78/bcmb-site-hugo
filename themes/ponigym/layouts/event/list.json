[
    {{ range $index, $e := (where .Data.Pages "Type" "event" ) }}
    {{ if $index }}, {{ end }}
    {
        "title": "{{ .Title }}",
        "start": "{{ dateFormat "2006-01-02" .Date }}",
        "allDay": true,
        "url": "{{ .Permalink | absURL }}"
    }
    {{ end }}
]